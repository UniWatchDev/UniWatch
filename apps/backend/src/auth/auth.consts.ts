/** HTTP-only cookie carrying the short-lived JWT access token. */
export const AUTH_ACCESS_COOKIE = 'access_token';

/** HTTP-only cookie carrying the opaque refresh token (server-side session). */
export const AUTH_REFRESH_COOKIE = 'refresh_token';
